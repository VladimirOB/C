#include "pch.h"

////
//////3.  Функция принимает 3 строки и заменяет в первой строке все вхождения второй строки на третью
//////char str[] = "Hello big big world big.";
//////replaceAll(str, "big", "small") -> "Hello small small world small."
////
////void str_change_one(char* st, char* st2, char* st3)
////{
////	for (int i = 0; i < st[i] != 0; i++)
////	{//st[i].replace(st.find("big"), 3, "small");
////
////		if (st[i] == st2[i])
////		{
////			st[i] = st3[i] ;
////
////		}
////	}
////}
////
////void main()
////{
////	char str[] = "Helo big big world big.";
////	char str2[] = "big";
////	char str3[] = "small";
////	for (int i = 0; i < str[i] != 0; i++)
////	{
////		cout << str[i];
////	}
////	str_change_one(str,str2,str3);
////	cout << "\nOTBET" << endl;
////	for (int i = 0; i < str[i] != 0; i++)
////	{
////		cout << str[i];
////	}
////}
//
